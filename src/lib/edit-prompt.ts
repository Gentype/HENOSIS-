/**
 * Edit Engine prompt — intentionally left empty.
 *
 * The hardcoded "apply incremental edits" instructions used to live
 * here. They have been removed at the user's request. The /api/edit
 * route now sends the user's edit instruction + prior files without a
 * pre-baked system message.
 */
export const EDIT_PROMPT = "";
