/**
 * Prompt Improver system prompt — intentionally left empty.
 *
 * The hardcoded "expand a short user idea" instructions used to live
 * here. They have been removed at the user's request. The
 * /api/improve-prompt route now sends only the user's text without a
 * pre-baked system message.
 */
export const IMPROVE_PROMPT = "";
