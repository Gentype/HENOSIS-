/**
 * Remix Engine system prompt — intentionally left empty.
 *
 * The hardcoded "clone an existing site into a better version"
 * instructions used to live here. They have been removed at the user's
 * request. The /api/remix route now sends only the scraped page +
 * user's extra direction without a pre-baked system message.
 */
export const REMIX_PROMPT = "";
